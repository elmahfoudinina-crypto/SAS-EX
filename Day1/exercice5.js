let text = "bonjour mes amis"
let count = 0 ;
for(let letter of text ){
    if(letter === "a" || letter === "o" || letter === "i" || letter === "u" || letter === "e" || letter === "A" || letter ==="O" || letter ==="I" || letter === "U" || letter === "E")
        count++; 

}
   console.log(count);